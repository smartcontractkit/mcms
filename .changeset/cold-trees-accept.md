---
"@smartcontractkit/mcms": minor
---

Add `ProposalKindFromJSON` and `ProposalKindFromJSONString` helpers

Adds two new functions to extract a `ProposalKind` from a JSON representation of a `Proposal` or `TimelockProposal`.
